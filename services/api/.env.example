# Waypoints API server configuration
# Copy to .env and adjust values

# Server
PORT=3000
HOST=0.0.0.0

# CORS – comma-separated origins, or "*" / "true" for allow-all (dev only)
CORS_ORIGIN=true

# Session limits
MAX_PARTICIPANTS_PER_SESSION=50
MAX_DISPLAY_NAME_LENGTH=30
